/**
 * Brand logo scroller — pure CSS marquee, no JS required.
 *
 * The animation is handled entirely by CSS (@keyframes lenvy-marquee).
 * Hover-pause is handled by CSS on .lenvy-brand-scroller-section:hover.
 * This module is kept as a placeholder for future enhancements.
 */

export function initBrandScroller() {
  // No-op — animation is CSS-driven.
}
